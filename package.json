{
  "name": "sonner-astro",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "check": "npm run check -w packages/sonner-astro && npm run check -w apps/web"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
